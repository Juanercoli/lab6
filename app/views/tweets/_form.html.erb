<%= simple_form_for @tweet do |t| %>
  <%= t.association :monster, collection: Monster.order(:name), prompt: "Elegi con que monstruo twittear"%>
  <%= t.input :content %>
  
  <%= t.button :submit %>
<% end %>